{
    "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && pnpm db:generate && NODE_ENV=production pnpm build:admin",
    "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
    "framework": "nextjs"
}
